# dependencies
node_modules/

# build artifacts
# lib/client.js is committed so a fresh clone works without a build step
dist/

# logs & OS junk
*.log
.DS_Store
Thumbs.db

# local settings — never commit real credentials
settings.yaml
*.local.yaml
